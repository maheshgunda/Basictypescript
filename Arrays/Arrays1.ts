// syntax: variabledeclaration arrayname: datatype[] =[Stored elements]

let stddata:number[] = [10,20,30,40,50];

console.log(stddata)

//print the first index of the array
console.log(stddata[0])

// print the length of the array
console.log(stddata.length)

